# MiniCalendar Holiday Data

Public, signed holiday data consumed by MiniCalendar. Files under `years/` are
transcribed only after checking the corresponding Chinese government notice.
`manifest.sig` is an Ed25519 signature over the exact bytes of `manifest.json`.

The signing private key is not stored in this directory or repository.
